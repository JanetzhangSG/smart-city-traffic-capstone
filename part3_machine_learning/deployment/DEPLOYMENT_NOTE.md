# FastAPI Deployment Simulation

The clean execution notebook tests the mock API using FastAPI's test client.

- `GET /health` -> HTTP 200
- `POST /predict` -> HTTP 200
- Model version: `task1-v1-hist_gradient_boosting_regressor`
- Clean Colab example prediction: **6432.68 vehicles/hour** for `2017-08-01T16:00:00`

The API is a deployment simulation for the capstone and is not presented as a production service.
